#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        ldconfig
        ;;
    purge)
        # Remove cache directory on purge
        rm -rf /var/cache/tdnf || true
        ldconfig
        ;;
esac

#DEBHELPER#

exit 0
